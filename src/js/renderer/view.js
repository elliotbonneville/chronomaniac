import buffer from "~/renderer/buffer";

export default class View {
	constructor(buffer, position, width, height) {
		this.position = position;
		this.width = width;
		this.height = height;
	}

	get bounds() {

	}

	get borders() {

	}

	get cells() {

	}
}